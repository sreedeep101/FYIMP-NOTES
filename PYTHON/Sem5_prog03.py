#Create a NumPy array and calculate its mean, sum, and maximum.

import numpy as np 

array = np.array([23,55,22,53,44,34])

print("array : " , array)
print("mean  : " , np.mean(array))
print("sum   : " , np.sum(array))
print("maximum : " , np.max(array))
